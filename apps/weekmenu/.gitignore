node_modules
.next
out
coverage
playwright-report
test-results
.data
.env
.env.local
*.tsbuildinfo
next-env.d.ts
.vercel

# Generated from src/data/seed by `pnpm seed:sql`. Anchored to the current
# week, so it is reproducible rather than versioned.
supabase/seed.sql

# Externe databron-momentopnames. Niet meegecommit: ze zijn groot (10 MB per
# stuk), veranderen dagelijks, en zijn met één commando opnieuw op te halen —
# zie data/external/README.md.
data/external/*.json

# Opgehaalde recept-corpora (92 MB). Brondata van derden, met `pnpm recipes:fetch`
# opnieuw op te halen; alleen de selectie in `_selected/` wordt meegecommit.
# Zie data/recipes/README.md.
data/recipes/candidates/forkrecipe/
data/recipes/candidates/open-recipe-archive/
data/recipes/candidates/public-domain-recipes/
data/recipes/candidates/recipe-dataset/
